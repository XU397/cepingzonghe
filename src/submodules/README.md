Submodules host optional or isolated feature modules.

Purpose
- Stage integrations that are not yet part of core `modules/`.
- Allow incremental migration to unified shared utilities.

Notes
- Prefer imports via aliases: `@submodules`, `@shared`, `@utils`.
