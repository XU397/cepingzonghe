App entry and shell for high-level composition.

Purpose
- App-level shell, providers, and layout wrappers.
- Minimal placeholder for gradual migration.

Notes
- Keep business pages under `src/pages/`.
- Use aliases like `@app`, `@shared`, `@hooks`.
