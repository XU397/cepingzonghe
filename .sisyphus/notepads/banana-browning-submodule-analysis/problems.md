## 2026-04-05

- Banana browning 页面 MD 与附录尚未提供完整的题目编码、答案键、完整题干、选项标签、事件埋点契约，当前 `src/submodules/g8-banana-browning-experiment/mapping.ts` 中相关映射仍为空。
- Page 12/13 等后续页面虽然有内容草案，但仍缺少代码级实现约束：表格行上限、星标唯一性、校验阈值、完成页是否提交空答案/是否直接触发 `onComplete`。
- 附录仍存在温度口径冲突记录（2/10/18℃ vs 20/25/30/35℃），实现前需要一个最终单一口径。
- `page-08` 仍缺一个产品级明确口径：六个香蕉 SVG 的黑变比例如何映射到可见视觉状态（连续遮罩/分档贴图/关键帧动画），以及初始/重置时数值框是空态还是显示 `0%`；这会直接影响面板状态机与视觉实现。
- `page-12` 的主要阻塞点不是表格结构，而是完成链路：提交成功后应进入 `page-13` 再由完成按钮结束，还是 `page-12` 直接结束任务、`page-13` 仅作展示；当前文档未把两页的提交/完成职责切开。
