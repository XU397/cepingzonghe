---
description: Legacy alias for OPSX propose.
---

# OpenSpec Proposal (Compatibility Stub)

This legacy workflow now maps to `/opsx:propose`. Use `.claude/commands/opsx/propose.md` or `.claude/skills/openspec-propose/SKILL.md` for the maintained instructions.

For proposal work, read `openspec/project.md`, create scoped artifacts under `openspec/changes/<change-id>/`, do not write implementation code, and validate strictly before sharing.
