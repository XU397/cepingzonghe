# 子模块页面制作信息需求模板

> 作者：AI交互工程师
> 基于：7年级蒸馒头子模块 (`grade-7-tracking`)
> 用途：批量制作类似结构的子模块页面时，明确每种页面类型所需的精确信息

---

## 页面类型分类体系

基于7年级蒸馒头模块的23个页面分析，按**复杂度**和**交互模式**分为7大类：

| 分类编号 | 页面类型 | 复杂度 | 典型页面示例 | 核心特征 |
|---------|---------|-------|------------|---------|
| **Type-A** | 基础信息展示页 | ⭐ | Page01, Page02, Page06, Page09 | 纯展示+倒计时/按钮 |
| **Type-B** | 单一文本输入页 | ⭐⭐ | Page03 | 一个textarea/input |
| **Type-C** | 多文本输入页 | ⭐⭐ | Page07 | 多个textarea（有验证） |
| **Type-D** | 复杂交互选择页 | ⭐⭐⭐ | Page04, Page08 | 弹窗/模态+复选/单选 |
| **Type-E** | 实验+选择题混合页 | ⭐⭐⭐⭐ | Page11-13 | 左侧实验区+右侧选择题 |
| **Type-F** | SVG动画实验页 | ⭐⭐⭐⭐⭐ | Page10 | 物理模拟+实时动画+历史记录 |
| **Type-G** | 标准化问卷页 | ⭐⭐ | Page15-22 | JSON驱动+表格布局 |

---

## Type-A：基础信息展示页

### 适用场景
- 注意事项、引导说明、情境引入、假设陈述、过渡说明、完成页

### 样例参考
- **Page01_Notice**（注意事项 + 40秒倒计时 + 复选框）
- **Page06_Hypothesis**（假设陈述 + 天气图片）
- **Page09_Transition**（过渡说明）

### 所需信息清单

#### 1. 基础元数据
```yaml
页面编号: "0.1" | "4" | "8"  # 用于pageNumber
页面标识: "intro_notice" | "hypothesis_statement"  # pageId
页面标题: "注意事项" | "蜂蜜变稀：假设陈述"  # h1标题
页面描述: "注意事项页" | "假设陈述"  # 用于pageDesc和日志
导航模式: "hidden" | "experiment"  # 决定是否显示左侧导航
```

#### 2. 内容结构
```yaml
布局类型:
  - single_column: 单列居中布局
  - split_horizontal: 左右分栏布局（左文字+右图片）
  - split_vertical: 上下布局

主要内容:
  - type: text  # 文字段落
    content: |
      作答时间：50分钟...
    style: plain | alert_box | highlight  # 样式变体

  - type: list  # 列表
    items:
      - "作答时间：**50分钟**（探究任务**40分钟** + 问卷调查**10分钟**）"
      - "请按顺序回答每页问题..."
    list_style: bullet | numbered | none

  - type: image  # 图片
    src: "天气图片.png"  # 相对路径（assets/images/）
    alt: "成都当月天气情况"
    position: left | right | center
    max_width: "400px" | "100%"

  - type: checkbox_agreement  # 确认复选框（可选）
    label: "我已阅读并理解以上注意事项"
    required: true
    disabled_until_timer: true  # 是否需要等待倒计时
```

#### 3. 交互逻辑
```yaml
倒计时:
  enabled: true | false
  duration: 40  # 秒
  on_complete:
    enable_checkbox: true  # 倒计时结束后启用复选框
    enable_next_button: false  # 是否直接启用下一页按钮

下一页按钮:
  enabled_condition: "checkbox_checked" | "timer_complete" | "always"
  button_text: "继续" | "下一页"
  target_page: 1  # 跳转到的页面编号（session.currentPage + N）

数据提交:
  answer_list:  # 如果有复选框或其他收集项
    - target_element: "notice_agreement"
      value: "已阅读并同意"
```

#### 4. 样式配置（可选）
```yaml
custom_styles:
  background_color: "#f9fafb"
  content_max_width: "800px"
  title_color: "#1f2937"
  alert_box_border: "2px solid #f59e0b"
```

---

## Type-B：单一文本输入页

### 适用场景
- 提出问题、简答题、开放式问答

### 样例参考
- **Page03_Question**（提出问题 - 左侧对话图片 + 右侧textarea）

### 所需信息清单

#### 1. 基础元数据
```yaml
页面编号: "2"
页面标识: "dialogue_question"
页面标题: "蜂蜜的奥秘"
页面描述: "提出问题"
导航模式: "experiment"
```

#### 2. 内容结构
```yaml
布局类型: split_horizontal  # 左右分栏

左侧内容:
  type: image
  src: "小明和爸爸对话.png"
  alt: "小明和爸爸的对话"
  description: "对话情境图"  # 可选的图片说明

右侧内容:
  type: text_input
  input_type: textarea  # textarea | input
  label: "根据左侧对话，请写出接下来小明要探究的科学问题？"
  placeholder: "请在此输入你的问题..."
  rows: 8
  max_length: 500  # 可选
  required: true
  validation:
    min_chars: 1  # 至少输入1个字符
    trim: true  # 提交前去除首尾空格
```

#### 3. 交互逻辑
```yaml
实时验证:
  on_start_typing:
    log_event: "text_input_start"
    target: "question_input"

  on_input_change:
    log_event: "text_input"
    debounce: false  # 是否防抖

下一页按钮:
  enabled_condition: "text_length > 0"  # 启用条件（表达式）
  disabled_message: "请输入您要探究的科学问题"
  button_text: "下一页"

数据提交:
  answer_list:
    - target_element: "question_input"
      value: "{user_input}"  # 占位符，表示用户输入的值
```

---

## Type-C：多文本输入页

### 适用场景
- 方案设计（多个想法输入）、多问简答题

### 样例参考
- **Page07_Design**（方案设计 - 3个textarea，每个至少2字）

### 所需信息清单

#### 1. 基础元数据
```yaml
页面编号: "5"
页面标识: "design_solution"
页面标题: "蜂蜜变稀：方案设计"
页面描述: "方案设计"
导航模式: "experiment"
```

#### 2. 内容结构
```yaml
说明文字:
  - "为验证小明的猜想，首先要确定评估蜂蜜黏度的方法。"
  - "**假设有两瓶蜂蜜，请你帮小明想一想，有哪些可以比较两瓶蜂蜜黏度的方法。**"
  - "请提出三个可能的想法，将其简要陈述在下方方框内。"

输入框列表:
  - id: "idea1"
    label: "1. 想法一："
    placeholder: "请输入你的想法..."
    rows: 8
    max_length: 500
    validation:
      min_chars: 2  # 最少2个字符
      required: true

  - id: "idea2"
    label: "2. 想法二："
    placeholder: "请输入你的想法..."
    rows: 8
    max_length: 500
    validation:
      min_chars: 2
      required: true

  - id: "idea3"
    label: "3. 想法三："
    placeholder: "请输入你的想法..."
    rows: 8
    max_length: 500
    validation:
      min_chars: 2
      required: true
```

#### 3. 交互逻辑
```yaml
实时验证:
  on_focus:  # 获得焦点时
    log_event: "start_edit"
    target: "实验想法输入框{N}"

  on_input:  # 输入时
    log_event: "文本域输入"
    log_value: "字符数：{length}"

综合验证:
  all_fields_valid: "idea1.length >= 2 && idea2.length >= 2 && idea3.length >= 2"
  invalid_message: "请完成所有三个想法的输入（每个至少2个字符）"

下一页按钮:
  enabled_condition: "{all_fields_valid}"
  button_text: "下一页"

数据提交:
  answer_list:
    - target_element: "实验想法1"
      value: "{idea1_value}"
    - target_element: "实验想法2"
      value: "{idea2_value}"
    - target_element: "实验想法3"
      value: "{idea3_value}"
```

---

## Type-D：复杂交互选择页

### 适用场景
- 资料阅读+因素选择、方案评价+单选

### 样例参考
- **Page04_Resource**（资料阅读 - 5个按钮弹窗 + 6个复选框）

### 所需信息清单

#### 1. 基础元数据
```yaml
页面编号: "3"
页面标识: "resource_reading"
页面标题: "蜂蜜变稀：资料阅读"
页面描述: "资料阅读"
导航模式: "experiment"
```

#### 2. 交互元素结构
```yaml
左侧：信息中心（模态弹窗按钮组）
  buttons:
    - id: "brewing"
      label: "蜂蜜酿造流程"
      icon: "🐝"  # 可选的emoji或图标
      modal_content:
        type: image  # image | text | mixed
        image_src: "蜂蜜酿造流程.png"
        alt: "蜂蜜酿造流程图"

    - id: "qa"
      label: "蜂蜜知识问答"
      icon: "💬"
      modal_content:
        type: image
        image_src: "蜂蜜知识问答.png"

    - id: "viscosity"
      label: "黏度原理揭秘"
      icon: "🔬"
      modal_content:
        type: image
        image_src: "黏度原理揭秘.png"

    - id: "storage"
      label: "蜂蜜储存说明"
      icon: "📦"
      modal_content:
        type: image
        image_src: "蜂蜜储存说明.png"

    - id: "adulteration"
      label: "掺假蜂蜜探析"
      icon: "🔍"
      modal_content:
        type: image
        image_src: "掺假蜂蜜探析.png"

右侧：任务说明+复选框组
  task_description: |
    为探究影响蜂蜜黏度的因素，小明搜集了左侧的五条资料。
    请点击并查看资料，思考蜂蜜黏度可能与以下哪些因素有关？
    单击选择你认为可能的选项，再次点击可取消选择（可多选）。

  options:
    - id: "temperature"
      label: "环境温度"
    - id: "humidity"
      label: "环境湿度"
    - id: "stirring"
      label: "人为搅拌"
    - id: "fermentation"
      label: "微生物发酵"
    - id: "pourSpeed"
      label: "倾倒速度"
    - id: "impurities"
      label: "掺入杂质"

  selection_type: checkbox  # checkbox | radio
  min_selections: 1  # 最少选择项数
  max_selections: 6  # 最多选择项数（null表示不限）
```

#### 3. 交互逻辑
```yaml
模态窗口交互:
  on_button_click:
    action: "open_modal"
    log_event: "resource_view"
    target: "resource_{button_id}"

  on_modal_close:
    log_event: "modal_close"
    target: "resource_{button_id}"

  track_viewed: true  # 是否跟踪已查看的资料

复选框交互:
  on_checkbox_toggle:
    log_event: "checkbox_toggle"
    target: "option_{option_id}"
    value: "选中" | "取消选中"

下一页按钮:
  enabled_condition: "selected_count >= 1"
  disabled_message: "请至少选择一个影响因素"
  button_text: "下一页"

数据提交:
  answer_list:  # 每个选中项单独记录
    - target_element: "factor_{option_id}"
      value: "{option_label}"
  # 示例：如果选了temperature和humidity
  # [{targetElement: "factor_temperature", value: "环境温度"},
  #  {targetElement: "factor_humidity", value: "环境湿度"}]
```

---

## Type-E：实验+选择题混合页

### 适用场景
- 实验分析（左侧可继续做实验，右侧回答选择题）

### 样例参考
- **Page11_Analysis1**（左侧IntegratedExperimentPanel + 右侧单选题）

### 所需信息清单

#### 1. 基础元数据
```yaml
页面编号: "9"
页面标识: "analysis_question_1"
页面标题: "实验分析(1/3)"
页面描述: "实验分析1"
导航模式: "experiment"
```

#### 2. 左侧：实验面板配置
```yaml
实验面板:
  component: "IntegratedExperimentPanel"  # 复用已有组件
  config:
    water_content_options: [15, 17, 19, 21]  # 含水量选项
    temperature_options: [25, 30, 35, 40, 45]  # 温度选项
    enable_history: false  # 是否显示历史记录（分析页通常不需要）
    enable_reset: true  # 是否显示重置按钮

  physics_model:
    calculation_method: "calculateFallTime"  # 使用的物理模型函数
    # calculateFallTime(waterContent, temperature) => fallTime
```

#### 3. 右侧：选择题配置
```yaml
问题卡片:
  question_number: "问题 1"
  question_text: "模拟实验表明，在**40℃**条件下，蜂蜜的含水量为多少时，小钢球的下落时间**最长**？"

  question_type: radio  # radio | checkbox

  options:
    - value: "15%"
      label: "15%"
    - value: "17%"
      label: "17%"
    - value: "19%"
      label: "19%"
    - value: "21%"
      label: "21%"

  required: true
```

#### 4. 交互逻辑
```yaml
实验交互:
  on_experiment_start:
    log_event: "simulation_timing_started"
    target: "计时开始按钮_分析页1"
    value: "温度{temperature}°C"

  on_experiment_complete:
    log_event: "simulation_run_result"
    target: "模拟实验运行结果"
    value:
      Run_ID: "run_Page_11_Analysis1_{timestamp}"
      Set_Temperature: "{temperature}"
      Results: [
        {WaterContent: 15, FallTime: 16.5},
        {WaterContent: 17, FallTime: 18.2},
        ...
      ]

选择题交互:
  on_answer_select:
    log_event: "单选"
    target: "实验分析"
    value: "{selected_option}"

下一页按钮:
  enabled_condition: "selected_answer != null"
  disabled_message: "请先回答问题"
  button_text: "下一页"

数据提交:
  answer_list:
    - target_element: "analysis_q1"
      value: "{selected_answer}"
```

---

## Type-F：SVG动画实验页

### 适用场景
- 物理模拟实验、动态交互实验

### 样例参考
- **Page10_Experiment**（4个量筒 + 温度控制 + 小钢球下落动画 + 实验历史）

### 所需信息清单

#### 1. 基础元数据
```yaml
页面编号: "8"
页面标识: "simulation_experiment"
页面标题: "蜂蜜变稀：模拟实验"
页面描述: "模拟实验"
导航模式: "experiment"
```

#### 2. 实验配置
```yaml
实验类型: "integrated_beaker_experiment"  # 集成量筒实验

量筒配置:
  count: 4  # 量筒数量
  labels: ["15%", "17%", "19%", "21%"]  # 量筒标签（含水量）
  height: 300  # 量筒高度（px）
  colors: ["#3b82f6", "#10b981", "#f59e0b", "#ef4444"]  # 量筒液体颜色

温度控制:
  type: "slider"  # slider | buttons
  range: [25, 30, 35, 40, 45]  # 温度选项（℃）
  default: 25
  step: 5
  controls:
    decrement_button: "减小温度（-5℃）"
    increment_button: "增加温度（+5℃）"

动画小球:
  color: "#1f2937"
  radius: 10  # px
  initial_position: "top"  # 量筒顶部
  animation_type: "physics_based"  # 基于物理模型的动画
  fps: 60  # 帧率

物理模型:
  function: "calculateFallTime"
  inputs: ["waterContent", "temperature"]
  output: "fallTime"  # 下落时间（秒）
  formula_description: |
    根据蜂蜜黏度公式计算小钢球的下落时间：
    fallTime = f(waterContent, temperature)

计时显示:
  format: "XX.X秒"
  precision: 1  # 小数位数
  position: "below_beaker"  # 在量筒下方

实验历史:
  enabled: true
  max_records: 10  # 最多保存10次实验记录
  display_format: "table"  # table | list
  columns:
    - "实验序号"
    - "温度（℃）"
    - "含水量15%"
    - "含水量17%"
    - "含水量19%"
    - "含水量21%"
```

#### 3. 左侧说明面板
```yaml
说明面板:
  标题: "模拟实验步骤如下："
  步骤列表:
    - "取含水量为15%, 17%, 19%, 21%的蜂蜜各200ml，分别放入4个量筒中；"
    - "将量筒放入恒温箱中，恒温箱温度可设为25℃, 30℃, 35℃, 40℃, 45℃；"
    - "释放量筒上方的小钢球，记录小钢球下落至量筒底部的总用时；"

  操作说明:
    - text: "单击"
      inline_images: ["减号.png", "加号.png"]
      description: "可调整温度；"
    - text: "设好温度后，单击"
      inline_images: ["计时开始.png"]
      description: "，小钢球会下落至量筒底部，量筒下方时间框显示下落总用时；"
    - text: "单击"
      inline_images: ["重置.png"]
      description: "可重新开始。"
```

#### 4. 交互逻辑
```yaml
温度调整:
  on_temperature_change:
    log_event: "temperature_adjust"
    target: "温度控制"
    value: "{new_temperature}℃"

实验启动:
  on_start_click:
    log_event: "simulation_timing_started"
    target: "计时开始按钮"
    value: "温度{temperature}°C"
    actions:
      - calculate_fall_times  # 计算所有量筒的下落时间
      - start_animations  # 启动4个小球的下落动画
      - disable_controls  # 禁用温度控制

实验完成:
  on_all_balls_landed:
    log_event: "simulation_run_result"
    target: "模拟实验运行结果"
    value:
      Run_ID: "run_Page_10_Experiment_{run_number}"
      Set_Temperature: "{temperature}"
      Results: [
        {WaterContent: 15, FallTime: 16.5},
        {WaterContent: 17, FallTime: 18.2},
        {WaterContent: 19, FallTime: 20.1},
        {WaterContent: 21, FallTime: 22.3}
      ]
    actions:
      - add_to_history  # 添加到实验历史记录
      - collect_answer  # 收集为答案
      - enable_controls  # 重新启用控制

重置实验:
  on_reset_click:
    log_event: "点击"
    target: "重置实验按钮"
    value: "重置实验"
    actions:
      - reset_balls_position
      - clear_timers
      - enable_controls

下一页按钮:
  enabled_condition: "experiment_history.length > 0"
  disabled_message: "请先完成至少一次实验"
  button_text: "下一页"

数据提交:
  answer_list:
    - target_element: "实验历史记录"
      value: "{JSON.stringify(experiment_history)}"
  # 包含所有实验记录：温度、含水量、下落时间
```

#### 5. 图片资源清单
```yaml
required_images:
  - path: "assets/images/减号71.png"
    usage: "温度减小按钮图标"
  - path: "assets/images/加号71.png"
    usage: "温度增加按钮图标"
  - path: "assets/images/计时开始71.png"
    usage: "计时开始按钮图标"
  - path: "assets/images/重置71.png"
    usage: "重置按钮图标"
  - path: "assets/images/时间框71.png"
    usage: "时间显示框背景"
```

---

## Type-G：标准化问卷页

### 适用场景
- 态度问卷、量表调查、标准化评估

### 样例参考
- **Page15-22_Questionnaire**（科学探究态度问卷 - 使用BaseQuestionnairePage + JSON数据驱动）

### 所需信息清单

#### 1. 基础元数据（在JSON中定义）
```json
{
  "totalQuestions": 61,
  "pages": [
    {
      "pageNumber": 14,
      "pageId": "questionnaire_page_1",
      "title": "科学探究态度问卷 (1/8)",
      "instructions": "1、你有多同意或不同意下列陈述？（请在每行中选择一个答案）",
      "questions": [...]
    }
  ]
}
```

#### 2. JSON数据结构规范
```json
{
  "pageNumber": 14,  // 必填：页码（用于路由和数据提交）
  "pageId": "questionnaire_page_1",  // 必填：页面唯一标识
  "title": "科学探究态度问卷 (1/8)",  // 必填：页面标题
  "instructions": "1、你有多同意或不同意下列陈述？（请在每行中选择一个答案）",  // 必填：顶部橙色说明框文字

  "questions": [  // 必填：问题列表
    {
      "id": 1,  // 必填：问题唯一ID（全局唯一，用于答案映射）
      "text": "我对许多不同事物有好奇心。",  // 必填：问题文本
      "type": "likert4",  // 必填：问题类型（likert4 | likert5 | binary | multiple_choice）
      "options": [  // 必填：选项列表
        {
          "value": "A",  // 必填：选项值（用于数据存储）
          "label": "非常不同意"  // 必填：选项标签（用于显示和提交）
        },
        {
          "value": "B",
          "label": "比较不同意"
        },
        {
          "value": "C",
          "label": "比较同意"
        },
        {
          "value": "D",
          "label": "非常同意"
        }
      ]
    },
    {
      "id": 2,
      "text": "我喜欢发问。",
      "type": "likert4",
      "options": [...]  // 同上
    }
    // ... 更多问题
  ]
}
```

#### 3. 问题类型定义
```yaml
支持的问题类型:
  likert4:
    描述: "4点李克特量表（非常不同意 → 非常同意）"
    选项数量: 4
    典型标签: ["非常不同意", "比较不同意", "比较同意", "非常同意"]

  likert5:
    描述: "5点李克特量表"
    选项数量: 5
    典型标签: ["非常不同意", "不同意", "中立", "同意", "非常同意"]

  binary:
    描述: "二元选择（是/否）"
    选项数量: 2
    典型标签: ["是", "否"]

  multiple_choice:
    描述: "多项选择（自定义选项）"
    选项数量: 2-6
    典型标签: 自定义
```

#### 4. 自动化行为
```yaml
组件行为（BaseQuestionnairePage自动处理）:
  - 页面进入时记录 page_enter 事件
  - 答案选择时记录 change 事件
  - 完成度实时验证（所有题目必须回答）
  - 点击下一页前检查完成度
  - 未完成时显示警告："请完成本页所有必答题后再继续"
  - 最后一页显示"提交问卷"按钮
  - 自动格式化提交数据（operationList + answerList）
  - 问卷首页（pageNumber==14）自动启动10分钟倒计时

数据提交格式:
  pageNumber: "14"
  pageDesc: "科学探究态度问卷 (1/8)"
  answerList: [
    {
      targetElement: "P14_问题1",
      value: "比较同意"  // 选项的label（不是value）
    },
    {
      targetElement: "P14_问题2",
      value: "非常同意"
    }
    // ... 每题一条记录
  ]
```

#### 5. 使用流程
```yaml
1. 准备JSON数据文件:
   路径: "src/modules/{submodule}/assets/data/questionnaire.json"

2. 创建页面组件（极简）:
   ```jsx
   import BaseQuestionnairePage from '../components/questionnaire/BaseQuestionnairePage';

   const Page15_Questionnaire1 = () => {
     return <BaseQuestionnairePage pageNumber={14} />;
   };

   export default Page15_Questionnaire1;
   ```

3. 注册到页面映射:
   在 mapping.ts 或 config.js 中添加页面路由

4. 完成！组件会自动处理所有交互和数据提交
```

---

## 通用信息（所有页面类型共享）

### 1. 日志记录规范
```yaml
必须记录的事件:
  page_enter:
    触发时机: useEffect挂载时
    target: "Page_{pageNumber}_{pageId}"
    value: "{page_description}"

  page_exit:
    触发时机: useEffect清理时
    target: "Page_{pageNumber}_{pageId}"
    value: "{page_description}" | "停留时长: {duration}秒"

  交互事件（根据页面类型）:
    - button_click / 点击
    - text_input / 文本域输入
    - checkbox_toggle / 复选框切换
    - radio_select / 单选
    - resource_view / 资料查看
    - modal_close / 模态窗口关闭
    - simulation_timing_started / 模拟实验计时开始
    - simulation_run_result / 模拟实验运行结果
```

### 2. 导航与数据提交
```yaml
导航标准流程:
  1. 验证：检查页面完成条件
  2. 阻断日志：如未完成，记录 click_blocked 事件
  3. 提交前日志：记录 button_click / 点击 事件
  4. 构建MarkObject：
     - pageNumber: String(session.currentPage)
     - pageDesc: PAGE_MAPPING[session.currentPage].desc
     - operationList: 当前页所有操作记录
     - answerList: 当前页收集的答案
     - beginTime: 页面进入时间（格式："YYYY-MM-DD HH:mm:ss"）
     - endTime: 提交时间（格式："YYYY-MM-DD HH:mm:ss"）
  5. 提交数据：await submitPageData(markObject)
  6. 清空操作：clearOperations()
  7. 导航：await navigateToPage(nextPageNumber)
```

### 3. 样式文件命名
```yaml
CSS Modules命名规范:
  - Page{NN}_{PageName}.module.css  # 单页面专用样式
  - CommonPageStyles.module.css     # 多页面共享样式（如ExplorationPages.module.css）
  - ComponentName.module.css        # 组件样式

类名命名约定:
  - 布局: .pageContainer, .splitLayout, .leftPanel, .rightPanel
  - 标题: .pageTitle, .sectionTitle, .questionHeader
  - 输入: .textArea, .checkboxOption, .optionLabel
  - 按钮: .nextButton, .resetButton, .infoButton
  - 状态: .active, .disabled, .selected
```

### 4. 图片资源管理
```yaml
图片存放路径:
  - 通用图片: "src/assets/images/{image_name}.png"
  - 模块专用: "src/modules/{submodule}/assets/images/{image_name}.png"

命名规范:
  - 中文命名: 允许（如"小明和爸爸对话.png"）
  - 文件格式: .png | .jpg | .svg
  - 尺寸建议:
    - 页面主图: 宽度800-1200px
    - 图标按钮: 24x24px 或 32x32px
    - 模态窗口图片: 宽度600-800px

引入方式:
  ```jsx
  import dialogueImage from '../../../assets/images/小明和爸爸对话.png';
  <img src={dialogueImage} alt="小明和爸爸的对话" />
  ```
```

---

## 快速参考：给AI工程师的信息清单模板

### 新建页面时，请提供以下信息：

```yaml
# 必填项
页面类型: Type-A | Type-B | Type-C | Type-D | Type-E | Type-F | Type-G
页面编号: "X"  # 用于pageNumber
页面标识: "page_id"  # 用于pageId（kebab-case）
页面标题: "XXX"  # h1标题
页面描述: "XXX"  # 用于pageDesc
导航模式: "experiment" | "questionnaire" | "hidden"

# 内容（根据页面类型提供对应章节的详细信息）
[参考上文对应类型的"所需信息清单"]

# 图片资源（如有）
图片列表:
  - 文件名: "XXX.png"
    用途: "XXX"
  - 文件名: "YYY.png"
    用途: "YYY"

# 特殊需求（可选）
特殊交互逻辑: "XXX"
自定义样式要求: "XXX"
验证规则: "XXX"
```

---

## 附录：7年级蒸馒头模块页面清单

| 页码 | pageId | 页面类型 | 复杂度 | 说明 |
|-----|--------|---------|-------|------|
| 0.1 | intro_notice | Type-A | ⭐ | 注意事项+倒计时+复选框 |
| 1 | intro | Type-A | ⭐ | 情境引入 |
| 2 | dialogue_question | Type-B | ⭐⭐ | 提出问题+textarea |
| 3 | resource_reading | Type-D | ⭐⭐⭐ | 资料阅读+复选框 |
| 4 | questionnaire_notice | Type-A | ⭐ | 问卷引入说明 |
| 5 | design_solution | Type-C | ⭐⭐ | 方案设计（3个textarea） |
| 6 | evaluation | Type-D | ⭐⭐⭐ | 方案评价+单选 |
| 7 | transition | Type-A | ⭐ | 过渡说明 |
| 8 | simulation_experiment | Type-F | ⭐⭐⭐⭐⭐ | 模拟实验+SVG动画 |
| 9 | analysis_question_1 | Type-E | ⭐⭐⭐⭐ | 实验分析1+单选 |
| 10 | analysis_question_2 | Type-E | ⭐⭐⭐⭐ | 实验分析2+单选 |
| 11 | analysis_question_3 | Type-E | ⭐⭐⭐⭐ | 实验分析3+单选 |
| 12 | summary | Type-A | ⭐ | 总结页 |
| 13 | solution_design | Type-C | ⭐⭐ | 解决方案设计 |
| 14 | questionnaire_page_1 | Type-G | ⭐⭐ | 问卷第1页（7题） |
| 15 | questionnaire_page_2 | Type-G | ⭐⭐ | 问卷第2页 |
| 16 | questionnaire_page_3 | Type-G | ⭐⭐ | 问卷第3页 |
| 17 | questionnaire_page_4 | Type-G | ⭐⭐ | 问卷第4页 |
| 18 | questionnaire_page_5 | Type-G | ⭐⭐ | 问卷第5页 |
| 19 | questionnaire_page_6 | Type-G | ⭐⭐ | 问卷第6页 |
| 20 | questionnaire_page_7 | Type-G | ⭐⭐ | 问卷第7页 |
| 21 | questionnaire_page_8 | Type-G | ⭐⭐ | 问卷第8页 |
| 22 | completion | Type-A | ⭐ | 完成页 |

---

## 使用说明

1. **确定页面类型**：根据交互复杂度选择 Type-A 到 Type-G
2. **填写信息模板**：按对应类型的"所需信息清单"逐项填写
3. **准备图片资源**：列出所需图片的文件名和用途
4. **提交给AI工程师**：完整的YAML信息 + 图片资源 = 可精准制作页面
5. **验收标准**：对照"通用信息"中的日志记录和导航流程进行测试

**重要提示**：
- Type-G（问卷页）只需提供JSON数据文件，页面组件极简（3行代码）
- Type-F（SVG动画实验）最复杂，需要物理模型函数配合
- Type-A（基础展示页）最简单，但要明确倒计时和复选框逻辑

---

**版本**：v1.0
**最后更新**：2025-01-19
**基于模块**：grade-7-tracking (7年级蒸馒头)
